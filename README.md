# WindowBase
## Project Description
This is a little windowing library.
It has three configurations, a dynamic library, a static library that links against the
dynamic CRT, and a static library that links against the static CRT.